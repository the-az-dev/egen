const { logHelp } = require("./help.action");
const { logVersion } = require("./version.action");
const { generate } = require("./generate.action");

module.exports = {
    logHelp,
    logVersion,
    generate
};
